#!/usr/bin/env bash

source ~/Sites/sebastiandedeyne/dotfiles/.zshrc

# General
alias http="python -m SimpleHTTPServer"
alias hosts="sudo vi /etc/hosts"

# Git
function bitbucket {
    git clone git@bitbucket.org:$1
}
alias bb="bitbucket"

# Vim
alias vi="/Applications/MacVim.app/Contents/MacOS/Vim"

# Go
export PATH="$PATH:/usr/local/go/bin"
export GOPATH=~/.go

# Homestead
alias hs="~/.composer/vendor/bin/homestead"

# Applications
alias sourcetree="open -a SourceTree"

function subl {
    if [ -z "$1" ]; then
        if [ ! -f ./${PWD##*/}.sublime-project ]; then
            open . -a "Sublime Text"
        else
            open ./${PWD##*/}.sublime-project
        fi
    else
        open $1 -a "Sublime Text"
    fi
}

# Add RVM to PATH for scripting
export PATH="$PATH:$HOME/.rvm/bin"

# MAMP
function mamp {
    rm /Applications/MAMP/htdocs
    ln -s $(cd "$1"; pwd) /Applications/MAMP/htdocs
}
